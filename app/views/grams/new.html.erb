<br />
<div class="col-10 offset-1 col-sm-8 offset-sm-2 col-md-6 offset-md-3 ml-auto mr-auto card form">
	<h2>new pic</h2>
	<%= simple_form_for @gram do |f| %>
		<br />
		<%= f.input :picture %>
		<br />
		<%= f.input :breed %>
		<%= f.input :message, label: 'Caption' %>
		<%= f.submit "share pic", class: "my-btn" %>
	<% end %>
</div>